.env
docker/redis/users.acl
# Local container state (postgres/redis/rabbitmq bind mounts)
data/
tree.txt
recordings/*

# Flow encryption keys (DO NOT COMMIT)
keys/

logs/

# Carrier codec probe holds live trunk credentials — keep it local only, never commit
cmd/codec_probe/main.go

# Generated per-trunk SIP configs — plaintext carrier passwords, never commit
docker/asterisk/

# Compiled test binaries. Windows App Control blocks executables run from the
# temp directory, so `go test` needs `-c -o .testbin/` to run here at all.
.testbin/
