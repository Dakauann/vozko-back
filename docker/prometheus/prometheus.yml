global:
  scrape_interval: 15s
  scrape_timeout: 10s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: "vozko-app"
    static_configs:
      - targets: ["localhost:9213"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]